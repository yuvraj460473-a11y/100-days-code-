#include <stdio.h>

int main() {
    int num1, num2;
    int sum;

    // Taking input from the user
    scanf("%d %d", &num1, &num2);

    // Calculating the sum
    sum = num1 + num2;

    // Displaying the result in the exact format required
    printf("Sum = %d\n", sum);

    return 0;
}
